<nav class="Hui-breadcrumb"><i class="icon-home"></i> 首页 <span class="c-gray en">&gt;</span> 管理员管理 <span class="c-gray en">&gt;</span> 权限分配 <a class="btn btn-success radius r mr-20" style="line-height:1.6em;margin-top:3px" href="javascript:location.replace(location.href);" title="刷新" ><i class="icon-refresh"></i></a></nav>
<div class="pd-20">
  <form class="Huiform" action="" method="post">
    <table class="table table-border table-bordered table-bg">
      <tbody>
        <tr>
          <td>
            <table class="table table-border table-bordered table-bg">
              <tbody>
              <?php foreach($arr as $item){?>
                <tr>
                  <th width="85">  <label class="item"><input name="ids[]" type="checkbox"  value="<?=$item['id']?>" <?php if(in_array($item['id'],$nodes)){ echo 'checked="checked"';}?>> <?=$item['title']?></label></th>
                  <td class="permission-list">
               
                    <div class="cl">
                    <?php foreach($item['child'] as $child){?>
                    <label class="item"><input name="ids[]" type="checkbox"    value="<?=$child['id']?>" <?php if(in_array($child['id'],$nodes)){ echo 'checked="checked"';}?>> <?=$child['title']?></label>
                    <?php }?>
                    </div>
                  </td>
                </tr>
                <?php }?>
              </tbody>
            </table>
          </td>
        </tr>
        
        <tr>
          <td>
            <button type="submit" class="btn btn-success radius"  > 修改</button>
          </td>
        </tr>
      </tbody>
    </table>
  </form>
</div>